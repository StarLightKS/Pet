# Pet
Detecting a pet's emotional state using photos or audio
```bash
cd Pet-Emotion-Recognition
pip install flask numpy pillow tensorflow werkzeug librosa
python create_model.py   # создать модель (один раз)
python app.py
```
